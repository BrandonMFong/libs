```
[] allow the ability to assert outside of the unit test function
[] allow two parameters like `BF_ASSERT(false, e.what())`
[x] error logs that gets pushed using assert
[x] better format for error dialog
```
